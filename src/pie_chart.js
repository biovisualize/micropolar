µ.PieChart = function module(){ return µ.PolyChart(); };

µ.PieChart.defaultConfig = function(){
    var config = {
        geometryConfig: {
            geometryType: 'pie'
        }
    };
    return config;
};